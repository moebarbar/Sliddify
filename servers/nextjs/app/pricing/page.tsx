import PricingPage from "@/components/Landing/PricingPage";

export const metadata = {
  title: "Pricing — Sliddify",
  description:
    "Simple, honest pricing for AI presentation generation. Start free, upgrade when you need unlimited decks.",
};

const Page = () => {
  return <PricingPage />;
};

export default Page;
